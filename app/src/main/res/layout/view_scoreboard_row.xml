<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="#9A83B6"
    app:cardCornerRadius="12dp"
    app:cardUseCompatPadding="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:paddingHorizontal="@dimen/wl_space_m">

        <!-- Rank (and/or star icon) -->
        <ImageView
            android:id="@+id/rankText"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginVertical="@dimen/wl_space_xs"
            android:adjustViewBounds="true"
            android:src="@drawable/star_icon_first"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/rankNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppins_bold"
            android:includeFontPadding="false"
            android:shadowColor="@color/wl_shadow"
            android:shadowDx="0"
            android:shadowDy="3"
            android:shadowRadius="3"
            android:text="1"
            android:textColor="#FFFFFF"
            app:layout_constraintBottom_toBottomOf="@id/rankText"
            app:layout_constraintEnd_toEndOf="@id/rankText"
            app:layout_constraintStart_toStartOf="@id/rankText"
            app:layout_constraintTop_toTopOf="@id/rankText" />

        <!-- Score (right aligned) -->
        <TextView
            android:id="@+id/textScore"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppins_bold"
            android:gravity="end"
            android:includeFontPadding="false"
            android:shadowColor="@color/wl_shadow"
            android:shadowDx="0"
            android:shadowDy="3"
            android:shadowRadius="3"
            android:text="15054"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/wl_text_title"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Name fills whatever is between rank and score -->
        <TextView
            android:id="@+id/textName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppins_bold"
            android:gravity="center"
            android:includeFontPadding="false"
            android:shadowColor="@color/wl_shadow"
            android:shadowDx="0"
            android:shadowDy="3"
            android:shadowRadius="3"
            android:text="Rowan"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/wl_text_title"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/textScore"
            app:layout_constraintStart_toEndOf="@id/rankText"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>


</androidx.cardview.widget.CardView>
